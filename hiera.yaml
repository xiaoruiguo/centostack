---
version: 4
datadir: hieradata
hierarchy: 
  - name: "Hostname"
    backend: yaml
    path: "hostname/%{facter.hostname}

  - name: "FQDN"
    backend: yaml
    path: "fqdn/%{facter.fqdn}

  - name: "common"
    backend: yaml
