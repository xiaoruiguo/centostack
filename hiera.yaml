---
version: 4
datadir: hieradata
hierarchy: 
  - name: "Hostname"
    backend: yaml
    path: "hostname/%{facter.hostname}"

  - name: "Fqdn"
    backend: yaml
    path: "nodes/%{facter.fqdn}"

  - name: "common"
    backend: yaml
